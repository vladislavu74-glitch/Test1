# Генерируется командой `xcodegen generate` — не коммитим, чтобы не тащить
# бинарный формат Xcode-проекта; source of truth — project.yml.
JobMonitor.xcodeproj/
JobMonitor/Info.plist

.DS_Store
xcuserdata/
DerivedData/
*.xcuserstate

# Локальный секрет (API-токен) для автозаполнения при первом запуске —
# см. JobMonitor/Networking/Secrets.swift.example. Реальный токен не должен
# попадать в публичный репозиторий.
JobMonitor/Networking/Secrets.swift
